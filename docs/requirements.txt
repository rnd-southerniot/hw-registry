# Doc-site build dependencies. Pinned to upper-bound on the next major
# so a future plugin breaking change can't sneak in via `latest`.
mkdocs-material>=9.6,<10
mkdocs-gen-files>=0.5.0,<1
mkdocs-literate-nav>=0.6.1,<1
mkdocs-section-index>=0.3.10,<1
mkdocs-macros-plugin>=1.3,<2
pymdown-extensions>=10.14,<11
mike>=2.1,<3
